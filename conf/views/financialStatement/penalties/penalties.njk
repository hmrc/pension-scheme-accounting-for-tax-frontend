{% extends "includes/layout.njk" %}

{% from "govuk/components/back-link/macro.njk"     import govukBackLink %}
{% from "govuk/components/button/macro.njk"        import govukButton %}
{% from "macros/title.njk"                         import title %}
{% from "govuk/components/table/macro.njk"         import govukTable %}
{% from "govuk/components/input/macro.njk"         import govukInput %}
{% from "govuk/components/fieldset/macro.njk"      import govukFieldset %}

{% from 'macros/inputSearch.njk'               import hmrcInputSearch with context %}


{% block pageTitle %}
  {{ title(titleMessage, form.errors) }}
{% endblock %}

{% block mainContent %}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">


        {% if schemeAssociated %}
            {{ hmrcPageHeading({
                  text: titleMessage,
                  section: schemeName
                }) }}
        {% else %}
        <h1 class="govuk-heading-xl">{{ titleMessage }}</h1>
        {% endif %}

      <p class="govuk-body">{{ messages("penalties.pstr", pstr) }}</p>
      <p class="govuk-body">{{ messages("penalties.p1") }}</p>

      <span id="penalities_charges_hidden" class="govuk-visually-hidden" aria-hidden="true"> {{ titleMessage }}</span>
     <div role="region" aria-labelledby="penalities_charges_hidden" class="govuk-!-margin-bottom-7" tabindex="0">
        {{ govukTable(table.penaltyTable) }}
     </div>


    </div>
  </div>

  <p class="govuk-body"><a id="return_to_psa" href="{{ config.managePensionsSchemeOverviewUrl }}" class="govuk-link">{{ messages("return.to.link", psaName) }}</a></p>

{% endblock %}
